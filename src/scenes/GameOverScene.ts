import Phaser = require("phaser");

export class GameOverScene extends Phaser.Scene{
    constructor(){
        super({
            key: 'GameOverScene'
        })
    }

    init(): void{

    }

    create(): void{

    }

    update(): void{
        
    }
}