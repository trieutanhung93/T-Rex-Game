import Phaser = require("phaser");

export class PlayGameScene extends Phaser.Scene{
    constructor(){
        super({
            key: 'PlayGameScene'
        })
    }

    init(): void{

    }

    create(): void{

    }

    update(): void{
        
    }
}